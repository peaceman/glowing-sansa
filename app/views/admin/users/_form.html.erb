<%= bootstrap_form_for([:admin, @user], :html => {:class => 'form-horizontal'}) do |f| %>
    <%= f.email_field :email %>
    <%= f.password_field :password %>
    <%= f.check_box :is_admin %>

    <%= f.actions %>
<% end %>